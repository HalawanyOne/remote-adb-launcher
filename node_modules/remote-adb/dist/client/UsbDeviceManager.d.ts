import { RemoteAdbDevice } from './RemoteAdbDevice';
declare class UsbDeviceManagerSingleton {
    private events;
    private connectedDevices;
    private _usb;
    private _usbInitialized;
    private get usb();
    constructor();
    isSupported: () => boolean;
    requestDevice: () => Promise<USBDevice>;
    private createRemoteAdbDevice;
    private refreshDevices;
    getDevices: () => Promise<RemoteAdbDevice[]>;
    monitorDevices: (callback: (devices: RemoteAdbDevice[]) => void) => void;
}
export declare const UsbDeviceManager: UsbDeviceManagerSingleton;
export {};
