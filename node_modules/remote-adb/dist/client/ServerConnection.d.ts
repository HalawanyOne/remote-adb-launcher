import WebSocket from 'isomorphic-ws';
export { WebSocket };
export type ServerStatus = {
    _error?: string;
    loginSupported?: boolean;
    loginRequired?: boolean;
};
export declare class ServerConnection {
    private serverAddress;
    private events;
    private updateLoopRunning;
    private lastServerStatusPromise;
    private cookieJar;
    private _fetch;
    constructor(address: string);
    private resolveUrl;
    fetch(url: string, init?: RequestInit): Promise<Response>;
    createWebSocket(url: string): Promise<WebSocket>;
    login: (password: string) => Promise<void>;
    monitorServerStatus: (callback: (status: ServerStatus) => void) => Promise<void>;
    getServerStatus: () => Promise<ServerStatus>;
    private updateStatusLoop;
    private fetchServerStatus;
}
