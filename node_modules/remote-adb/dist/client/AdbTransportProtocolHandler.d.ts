export type LoggerConfig = {
    tag: string;
    direction: "<==" | "==>";
};
export declare class AdbTransportProtocolHandler {
    private static getPayloadLength;
    private static concatBuffers;
    static startPullPushLoop(isConnected: () => boolean, pull: (length: number) => Promise<ArrayBuffer>, push: (buffer: ArrayBuffer) => Promise<void>, loggerConfig: LoggerConfig): Promise<void>;
    static createDataEventHandler(push: (buffer: ArrayBuffer) => Promise<void>, loggerConfig: LoggerConfig): (data: ArrayBuffer) => void;
}
export default AdbTransportProtocolHandler;
