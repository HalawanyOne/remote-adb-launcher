import { EventEmitter } from "events";
import { AdbTransport } from "./AdbTransport";
import { ServerConnection } from "./ServerConnection";
export declare class RemoteAdbDevice extends EventEmitter {
    private backend;
    private ws;
    private serverHandshake;
    private _connecting;
    get serial(): string;
    get name(): string;
    get remoteSerial(): string | undefined;
    get bytesTransferred(): {
        up: number;
        down: number;
    };
    constructor(backend: AdbTransport);
    get connected(): boolean;
    get connecting(): boolean;
    connect: (serverConnection: ServerConnection) => Promise<void>;
    private disconnectBackend;
    private disconnectWebSocket;
    disconnect: () => Promise<void>;
}
