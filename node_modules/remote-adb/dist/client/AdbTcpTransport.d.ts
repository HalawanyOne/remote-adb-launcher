import { EventEmitter } from 'events';
import { AdbTransport, WebSocket } from './AdbTransport';
export declare class AdbTcpTransport implements AdbTransport {
    private host;
    private port;
    private socket;
    readonly type = "TCP";
    private _serial;
    get serial(): string;
    private _name;
    get name(): string;
    private _connected;
    get connected(): boolean;
    private _bytesTransferred;
    get bytesTransferred(): {
        up: number;
        down: number;
    };
    private readonly events;
    readonly ondisconnect: (listener: (e: Event) => void) => EventEmitter;
    constructor(serial: string, name: string, host: string, port: number);
    private handleClose;
    connect(): Promise<void>;
    pipe(ws: WebSocket): Promise<void>;
    dispose(): Promise<void>;
}
