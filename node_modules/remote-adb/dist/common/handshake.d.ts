import type WebSocket from 'isomorphic-ws';
export type ClientHandshake = {
    type: "handshake";
    name: string;
    serial: string;
};
export type ServerHandshake = {
    type: "handshake";
    serial: string;
};
export declare function getRemoteHandshake<T extends ClientHandshake | ServerHandshake>(ws: WebSocket): Promise<T>;
