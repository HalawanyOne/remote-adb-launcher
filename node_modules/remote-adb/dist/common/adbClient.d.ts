import * as net from 'net';
export declare class AdbClient {
    private socket;
    connect(port?: number, host?: string): Promise<net.Socket>;
    private read;
    private writeRequest;
    readMessage(): Promise<string | null>;
    private readResponse;
    request(service: string): Promise<void>;
}
